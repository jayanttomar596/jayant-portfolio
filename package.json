{
  "name": "jayant-tomar-portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "Vercel-ready portfolio for Jayant Tomar.",
  "scripts": {
    "dev": "python3 -m http.server 5173",
    "build": "rm -rf dist && mkdir -p dist && cp index.html dist/ && cp -R src dist/src && cp -R public/. dist/",
    "preview": "python3 -m http.server 4173 --directory dist"
  }
}
